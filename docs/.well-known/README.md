# .well-known

This directory is for .well-known resources (for example: `security.txt`, `webfinger`, `nodeinfo`) to be published under `/.well-known/` on the site generated from `docs/`.

It is intentionally empty for now. Add protocol-specific files here when needed.
