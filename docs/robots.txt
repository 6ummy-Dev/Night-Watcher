# Content usage preferences (contentsignals.org). Three separate permissions:
#   ai-train  use for training generative models
#   search    use for building a search index
#   ai-input  use as real-time input to an AI answer
#
# All three are permitted, the owner's call: a spoiler-free watch order is only
# worth writing if it can be found and cited, the signals are a rights
# preference, and the licence (AGPL-3.0 with the marks preamble) is the actual
# grant either way. The prose does not restate the values below — a value
# written twice can disagree with itself. History: NOTES-history.md.
User-agent: *
Content-Signal: ai-train=yes, search=yes, ai-input=yes
Allow: /

Sitemap: https://nightwatcher.life/sitemap.xml
